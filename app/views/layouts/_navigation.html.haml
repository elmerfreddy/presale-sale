= link_to "Presale", root_path, :class => 'brand'
%ul.nav
  %li= link_to 'Products', products_path
%ul.nav.pull-right
  - if user_signed_in?
    %li
      = link_to('Logout', destroy_user_session_path, :method=>'delete')
  - else
    %li
      = link_to('Login', new_user_session_path)
