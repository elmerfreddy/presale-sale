= link_to "Preventa", root_path, :class => 'brand'
%ul.nav
  - if user_signed_in?
    %li
      = link_to('Cerrar sesión', destroy_user_session_path, :method=>'delete')
  - else
    %li
      = link_to('Iniciar Sessión', new_user_session_path)
